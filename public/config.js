// Значение подставляется entrypoint-скриптом контейнера при старте
// (см. Dockerfile) — реальный API_BASE_URL берётся из переменной окружения
// контейнера, а не «зашивается» при сборке образа.
window.__HOKKER_CONFIG__ = {
  API_BASE_URL: "__API_BASE_URL__",
};
